<h1>ユーザー一覧</h1>
<p>CSVダウンロード</p>

<table>
	<thead>
	<tr>
		<th>ユーザーID</th>
		<th>キャンペーン参加</th>
	</tr>
	</thead>

<% @user.each do |user| %>

	<tr>
		<td><%= user.user_id %></td>
		<td><% if user.campaign_flag == true %>
			<%= "有" %>
			<% else %>
			<%= "無" %>
			<% end %>			
		</td>
	</tr>

<% end %>

</table>
